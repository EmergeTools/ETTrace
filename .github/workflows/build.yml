name: Pull Request Build

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - feature/spm

jobs:
  test:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Build ETTrace for iOS Simulator
        run: xcodebuild build -scheme ETTrace -sdk iphonesimulator -destination 'ANY=A'
      - name: Build ETTrace for iOS
        run: xcodebuild build -scheme ETTrace -sdk iphoneos -destination 'ANY=A'
      - name: Build ETTrace for macOS
        run: xcodebuild build -scheme ETTraceRunner -sdk macosx -destination 'ANY=A' ONLY_ACTIVE_ARCH=NO